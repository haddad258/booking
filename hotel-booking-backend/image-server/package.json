{
  "name": "hotel-booking-image-server",
  "version": "1.0.0",
  "description": "Dedicated image-serving app, separate from the main API so it can later be swapped for a CDN/object-storage origin without touching backend/ or either frontend beyond an env var.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
